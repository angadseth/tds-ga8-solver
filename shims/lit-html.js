// Stub. The exam bundle imports these only to render question HTML; the solver
// calls the generator functions, which never touch them.
export const html = (strings, ...values) => ({ strings, values });
export const render = () => {};
export const nothing = "";
